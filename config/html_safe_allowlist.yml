# Approved .html_safe / raw() calls.
#
# Each entry is "relative/path:line_number". The lint:html_safe rake task
# will fail if any .html_safe or raw() call is found that is NOT in this list.
#
# Before adding a new entry, verify the call is safe:
#   - All interpolated user content must be escaped with ERB::Util.html_escape
#   - Hardcoded values (integers, literal strings) are safe without escaping
#   - Renderer output (Redcarpet with escape_html: true) is safe
#   - ScalableNumberPreprocessor output is safe (escapes user content internally)

# RecipesHelper — renderer and preprocessor output (safe by construction)
- "app/helpers/recipes_helper.rb:6"     # header comment mentions .html_safe (not a call)
- "app/helpers/recipes_helper.rb:21"    # render_markdown: Redcarpet with escape_html: true
- "app/helpers/recipes_helper.rb:28"    # scalable_instructions: preprocessor escapes internally
- "app/helpers/recipes_helper.rb:34"    # format_yield_line: preprocessor escapes internally
- "app/helpers/recipes_helper.rb:40"    # format_yield_with_unit: preprocessor escapes internally

# RecipesController — show_html: Redcarpet output (escape_html: true), title escaped via ERB::Util
- "app/controllers/recipes_controller.rb:74"

# _step.html.erb
- "app/views/recipes/_step.html.erb:44" # processed_instructions: pre-rendered HTML from parse-on-save

# _shopping_list.html.erb — title attribute with user content escaped by h()
- "app/views/groceries/_shopping_list.html.erb:18"

# _nutrition_table.html.erb — hardcoded literals, no user content
- "app/views/recipes/_nutrition_table.html.erb:15" # indent class: hardcoded integer
- "app/views/recipes/_nutrition_table.html.erb:18" # nutrient data attrs: hardcoded key + numeric value

# nutrition_tui — terminal UI, not web HTML; Text::Span.raw is safe
- "lib/nutrition_tui/screens/ingredient.rb:465"
